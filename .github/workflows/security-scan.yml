# name: ShieldFlow Security Pipeline

# on:
#   push:
#   pull_request:

# jobs:
#   semgrep-scan:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout repo
#         uses: actions/checkout@v4

#       - name: Setup Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: "3.12"

#       - name: Install Semgrep
#         run: pip install semgrep

#       - name: Run Semgrep scan
#         run: |
#           semgrep --config auto . --json > semgrep.json

#       - name: Show scan summary (debug)
#         run: |
#           cat semgrep.json

#       - name: Upload Semgrep artifact
#         uses: actions/upload-artifact@v4
#         with:
#           name: semgrep-report
#           path: semgrep.json

#       # ----------------------------------------------------
#       # 🚨 FUTURE: ingestion into ShieldFlow API
#       # ----------------------------------------------------
#       # Uncomment this AFTER you expose your API via ngrok or cloud
#       #
#       # - name: Send to ShieldFlow ingestion API
#       #   run: |
#       #     curl -X POST "https://YOUR_PUBLIC_API/ingest/semgrep"
#       # ----------------------------------------------------


name: ShieldFlow Security Pipeline

on:
  push:
  pull_request:

jobs:
  security-scan:
    runs-on: ubuntu-latest

    steps:
      # ----------------------------------------------------
      # 1. Checkout code
      # ----------------------------------------------------
      - name: Checkout repo
        uses: actions/checkout@v4

      # ----------------------------------------------------
      # 2. Setup Python (for Semgrep)
      # ----------------------------------------------------
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      # ----------------------------------------------------
      # 3. Install Semgrep
      # ----------------------------------------------------
      - name: Install Semgrep
        run: pip install semgrep

      # ----------------------------------------------------
      # 4. Run Semgrep (SAST)
      # ----------------------------------------------------
      - name: Run Semgrep scan
        run: |
          semgrep --config auto . --json > semgrep.json

      - name: Upload Semgrep artifact
        uses: actions/upload-artifact@v4
        with:
          name: semgrep-report
          path: semgrep.json

      # ----------------------------------------------------
      # 5. Install Gitleaks (Secrets scanning)
      # ----------------------------------------------------
      - name: Install Gitleaks
        run: |
          curl -sSL https://github.com/gitleaks/gitleaks/releases/latest/download/gitleaks-linux-amd64 -o gitleaks
          chmod +x gitleaks

      # ----------------------------------------------------
      # 6. Run Gitleaks (Secrets detection)
      # ----------------------------------------------------
      - name: Run Gitleaks scan
        run: |
          ./gitleaks detect --source . --report-format json --report-path gitleaks.json || true

      - name: Upload Gitleaks artifact
        uses: actions/upload-artifact@v4
        with:
          name: gitleaks-report
          path: gitleaks.json

      # ----------------------------------------------------
      # 7. Debug output (optional but useful now)
      # ----------------------------------------------------
      - name: Show Semgrep output
        run: cat semgrep.json

      - name: Show Gitleaks output
        run: cat gitleaks.json

      # ----------------------------------------------------
      # 8. Future: ingestion into ShieldFlow API (SOAR layer)
      # ----------------------------------------------------
      # Uncomment when ngrok / deployed API is ready
      #
      # - name: Send Semgrep results to API
      #   run: |
      #     curl -X POST "https://YOUR_NGROK_URL/ingest/semgrep"
      #
      # - name: Send Gitleaks results to API
      #   run: |
      #     curl -X POST "https://YOUR_NGROK_URL/ingest/gitleaks"
      # ----------------------------------------------------